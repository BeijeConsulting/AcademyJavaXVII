const dayOfWeekUtils = require('../Utils/dayOfWeekUtils');

module.exports = {
    getDaysOfWeekBySchedule: function(schedule){
        return dayOfWeekUtils.getDaysOfWeekBySchedule(schedule);
    },
}